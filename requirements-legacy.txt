-r requirements.txt

# Optional dependencies for archived / historical scripts.
#
# Do not install this for the current SharedSAE mainline unless you explicitly
# need old files under research/archive_experiments, evalscripts, trainscripts,
# or the historical SAE dataset iterator.

imageio==2.33.0
nudenet==3.4.2
pandas==2.2.3
requests==2.32.3
webdataset>=0.2.86

# Some historical scripts also depend on external packages or repos that are not
# published in this repository's default environment, for example `sld`.
