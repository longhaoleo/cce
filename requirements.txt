# Core dependencies for the current SDXL + WSAE concept erasure workflow.
# This covers Shared training, Shared concept locating, Shared erasure,
# and prompt-conditioned Shared blacklist mining.

diffusers>=0.34.0
matplotlib>=3.10.0
numpy>=2.2.0
Pillow>=11.0.0
safetensors>=0.5.0
seaborn>=0.13.0
torch>=2.6
tqdm>=4.66.0
transformers>=4.48.0
